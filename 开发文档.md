# 第五组系统协调层开发文档（企业级）

## 1. 文档信息

- 文档名称：第五组系统协调层开发文档
- 适用范围：Linux Agentic OS 课程项目第 5 组
- 版本：v1.0
- 维护人：第 5 组全体成员
- 生命周期：第 1 周至第 4 周交付期

---

## 2. 项目背景与目标

### 2.1 背景

本项目基于 UFO² 架构，在 Linux 桌面环境实现自然语言驱动的 Agentic OS 原型。第 5 组负责系统协调层，承担跨模块编排、安全控制、知识沉淀三类关键能力。

### 2.2 目标

第 5 组目标如下：

1. 建立统一执行编排能力，串联第 1/2/3/4 组模块。
2. 建立可审计、可解释的安全检查机制，拦截高风险操作。
3. 建立执行轨迹知识库，支持历史检索与经验复用。
4. 确保模块具备工程可交付性：可测试、可观测、可集成、可演示。

### 2.3 非目标（Out of Scope）

1. 不负责第 1 组意图识别模型优化。
2. 不负责第 2 组控件检测算法细节优化。
3. 不负责第 3 组 GUI 自动化动作执行逻辑实现。
4. 不负责第 4 组工具内部业务逻辑实现。

---

## 3. 组织分工（4 人）

### 3.1 角色与职责

- 成员 A（集成负责人）
  - 主责：`SystemCoordinator`、全链路编排、跨组接口治理、集成收口。
  - 备份：第 1/2 组接口适配策略。

- 成员 B（安全负责人）
  - 主责：`SecuritySandbox`、规则引擎、风险分级、拦截机制。
  - 备份：安全审计字段定义。

- 成员 C（知识库负责人）
  - 主责：`RAGKnowledgeBase`、轨迹入库、检索与召回策略。
  - 备份：案例归档与经验复用模板。

- 成员 D（质量与联调负责人）
  - 主责：测试框架、联调脚本、回归验证、演示稳定性。
  - 备份：集成阻塞问题专项支援。

### 3.2 RACI（简化）

- 需求与接口冻结：A 负责，B/C/D 参与。
- 安全策略发布：B 负责，A 审核，D 验证。
- 轨迹模型发布：C 负责，A 审核，D 验证。
- 集成版本发布：A 负责，D 验证，B/C 会签。

---

## 4. 系统架构设计

### 4.1 分层定位

第 5 组属于系统协调层，位于用户交互层与执行层之间，负责“统一调度 + 安全准入 + 轨迹沉淀”。

### 4.2 逻辑架构

1. 输入入口：接收第 1 组 `intent_json`。
2. 安全准入：调用 `SecuritySandbox` 输出 `check_json`。
3. 编排执行：通过 `SystemCoordinator` 调用第 2/3/4 组。
4. 结果汇聚：统一状态、错误、耗时、风险等级。
5. 轨迹沉淀：写入 `RAGKnowledgeBase` 与审计日志。
6. 输出返回：返回标准化执行响应。

### 4.3 端到端时序（标准）

1. 用户输入自然语言。
2. 第 1 组输出结构化意图。
3. 第 5 组执行安全检查。
4. 安全通过后，调用第 2 组生成步骤与元素。
5. 调用第 3 组执行步骤，必要时调用第 4 组工具。
6. 第 5 组收集结果并记录审计、轨迹。
7. 返回执行结果与风险信息。

---

## 5. 模块详细设计

## 5.1 `SystemCoordinator`

### 职责

1. 模块注册与健康检查。
2. 标准执行编排（含超时控制、重试策略、失败回滚策略）。
3. 统一错误码与响应封装。

### 建议接口

- `register(module_id: str, module: object) -> None`
- `health_check() -> dict`
- `orchestrate(intent_json: dict) -> dict`

### 关键规则

1. 任一关键阶段失败需返回可解释错误。
2. 必须在返回前写入审计记录。
3. 所有跨组调用必须带 `trace_id`。

## 5.2 `SecuritySandbox`

### 职责

1. 权限校验（动作级、路径级、上下文级）。
2. 风险分级（low/medium/high/critical）。
3. 拦截危险指令与受保护资源访问。

### 建议接口

- `check_permission(action: str, target: str, params: dict) -> dict`
- `risk_score(action: str, target: str, params: dict) -> dict`

### 基线策略

1. 拦截高危命令：`rm`、`format`、`shutdown`、`dd`。
2. 拦截受保护路径：`/etc`、`/usr`、`/root`、`/boot`。
3. 对命令拼接、路径绕过进行额外检测。

## 5.3 `RAGKnowledgeBase`

### 职责

1. 记录执行轨迹（输入、计划、执行结果、风险、耗时）。
2. 提供检索能力（最近记录 + 相似任务召回）。
3. 支持问题复盘与演示证据追溯。

### 建议接口

- `add_trace(record: dict) -> str`
- `query(text: str, k: int = 3) -> list`
- `stats() -> dict`

### 数据策略

1. 第 1 阶段：先保证“可存可查”。
2. 第 2 阶段：补充简单向量化检索（基于 `numpy`）。
3. 第 3 阶段：按风险等级与场景标签聚类。

## 5.4 `AuditLogger`

### 职责

1. 记录全链路审计信息。
2. 支持按 `trace_id` 检索完整请求。
3. 支持异常追踪与回放分析。

### 必填字段

- `trace_id`
- `timestamp`
- `user_input`
- `intent`
- `security_result`
- `execution_result`
- `duration_ms`
- `error_code`

---

## 6. 接口契约规范

### 6.1 第 1 组 -> 第 5 组输入（`intent_json`）

```json
{
  "trace_id": "uuid",
  "intent": "文件操作|应用控制|系统设置|信息查询",
  "target": "string",
  "action": "string",
  "params": {},
  "user_input": "string",
  "timestamp": "ISO-8601"
}
```

### 6.2 第 5 组安全输出（`check_json`）

```json
{
  "trace_id": "uuid",
  "approved": true,
  "risk_level": "low|medium|high|critical",
  "reason": "string",
  "policy_id": "SEC-XXX"
}
```

### 6.3 第 5 组最终输出（`orchestrate_result`）

```json
{
  "trace_id": "uuid",
  "success": true,
  "stage": "planning|execution|tool_call|completed",
  "risk_level": "low|medium|high|critical",
  "result": {},
  "error": {
    "code": "E0000",
    "message": ""
  },
  "duration_ms": 1200
}
```

### 6.4 错误码规范

- `E1001`：安全策略拒绝。
- `E2001`：任务规划失败（第 2 组接口失败）。
- `E3001`：执行失败（第 3 组返回失败）。
- `E4001`：工具调用失败（第 4 组返回失败）。
- `E5001`：系统协调器内部异常。
- `E5002`：超时。

---

## 7. 数据模型设计

### 7.1 轨迹记录模型（`trace_record`）

```json
{
  "trace_id": "uuid",
  "user_input": "string",
  "intent_json": {},
  "check_json": {},
  "plan_json": {},
  "result_json": {},
  "risk_level": "low|medium|high|critical",
  "success": true,
  "duration_ms": 1200,
  "created_at": "ISO-8601"
}
```

### 7.2 审计日志模型（`audit_event`）

```json
{
  "trace_id": "uuid",
  "event": "SECURITY_CHECK|PLAN_READY|EXEC_DONE|FAILED",
  "detail": {},
  "timestamp": "ISO-8601"
}
```

### 7.3 安全策略模型（`security_policy`）

```json
{
  "policy_id": "SEC-001",
  "action_pattern": "rm|shutdown|dd",
  "target_pattern": "/etc|/usr|/root|/boot",
  "risk_level": "critical",
  "enabled": true
}
```

---

## 8. 非功能性要求（NFR）

### 8.1 性能

1. 单次标准任务编排耗时目标：`<= 3s`（不含外部模型波动）。
2. 安全检查耗时目标：`<= 100ms`。
3. 日志写入与轨迹入库不阻塞主流程（可异步化）。

### 8.2 稳定性

1. 核心流程异常需可恢复并返回标准错误。
2. 任意模块不可用时提供降级策略（可切换替代实现）。
3. 演示关键路径连续 20 次执行成功率应达到 `>= 95%`。

### 8.3 可观测性

1. 所有请求必须有 `trace_id`。
2. 提供阶段级耗时指标。
3. 关键失败需输出可定位日志。

### 8.4 可维护性

1. 统一代码风格（PEP8）。
2. 公共接口与数据结构必须集中维护。
3. 模块内聚、跨模块低耦合。

---

## 9. 安全与合规要求

1. 默认拒绝高危动作，采用“白名单优先 + 黑名单兜底”。
2. 对用户输入执行基础净化，防止命令注入样式字符串进入执行层。
3. 审计日志保留完整执行证据，确保可追责。
4. 禁止在真实系统执行破坏性测试，危险策略仅在沙箱场景验证。

---

## 10. 测试策略

### 10.1 测试分层

1. 单元测试：协调器流程分支、安全策略命中、轨迹入库检索。
2. 集成测试：与第 1/2/3/4 组接口联调。
3. 端到端测试：覆盖任务书验收关键场景。
4. 安全测试：高危命令、受保护路径、绕过样本。

### 10.2 覆盖目标

1. 核心模块单元测试覆盖率目标：`>= 80%`。
2. 关键接口异常分支覆盖率：`>= 90%`。
3. 验收场景通过率：`>= 95%`。

### 10.3 回归机制

1. 每次接口变更触发回归测试。
2. 每周至少一次全链路回归。
3. 演示前执行冻结版本回归。

---

## 11. 4 周实施计划（企业化排期）

## 第 1 周：需求澄清与接口冻结

### 目标

完成架构基线、接口冻结、最小链路打通。

### 任务

- A：编排主流程骨架、`trace_id` 贯通。
- B：首版安全规则与分级模型。
- C：轨迹模型与存储接口。
- D：联调脚手架与基础测试模板。

### 周交付

1. 接口契约文档（字段冻结）。
2. 系统协调层 MVP 可运行版本。
3. 检查点 1 所需材料基础。

### 退出标准

- `1 -> 5 -> 2 -> 3 -> 4` 主链路可跑通。
- 安全拒绝与放行都可复现。

## 第 2 周：核心能力实现

### 目标

将 MVP 升级为可用版本。

### 任务

- A：补齐超时、重试、异常封装。
- B：扩展风险策略与审计字段。
- C：实现可查询知识库（基础召回）。
- D：建设单测与联调测试用例。

### 周交付

1. 核心模块代码。
2. 单元测试与联调报告。
3. 检查点 2 交付材料。

### 退出标准

- 高危动作可稳定拦截。
- 普通场景执行成功率满足目标。

## 第 3 周：跨组联调与优化

### 目标

完成全链路联调，收敛缺陷。

### 任务

- A/D：主导跨组联调与问题闭环。
- B：补强绕过风险样本防护。
- C：增强轨迹字段与检索命中质量。

### 周交付

1. 联调缺陷清单与修复记录。
2. 关键场景演示脚本。
3. 检查点 3 材料。

### 退出标准

- 关键验收场景可连续通过。
- 关键错误可追踪到具体阶段。

## 第 4 周：验收冲刺与发布

### 目标

冻结版本、准备验收与答辩演示。

### 任务

- A：发布候选版本（RC）与最终版本。
- B：安全机制演示与证据整理。
- C：知识库检索演示准备。
- D：全链路回归与稳定性测试。

### 周交付

1. 最终集成版本。
2. 演示脚本与录屏材料。
3. 最终验收资料包。

### 退出标准

- 满足课程验收标准：可集成、可拦截、可追溯、可演示。

---

## 12. 风险管理

### 12.1 关键风险

1. 跨组接口频繁变更导致联调延迟。
2. 安全策略过严导致误拦截。
3. RAG 召回效果不稳定影响演示。
4. 关键成员并行任务过多导致瓶颈。

### 12.2 应对策略

1. 第 1 周冻结接口，变更必须登记。
2. 安全策略分级灰度：先拦高危，再放宽中风险。
3. 检索采用“双通道”：关键词 + 相似度。
4. D 作为机动位优先支援阻塞任务。

---

## 13. 协作与沟通机制

1. 每日站会：15 分钟（进展、阻塞、风险）。
2. 每日固定联调窗口：30-60 分钟。
3. 每周评审会：接口状态、缺陷状态、里程碑达成率。
4. 问题闭环流程：发现 -> 归类 -> 定责 -> 修复 -> 回归 -> 关闭。

---

## 14. 验收清单（第 5 组）

1. 系统协调器可完成全链路编排。
2. 安全沙箱可拦截危险操作并输出风险等级。
3. 审计日志可追溯一次任务全流程。
4. RAG 知识库可查询历史执行轨迹。
5. 关键场景演示稳定通过。
6. 单元测试、联调测试、回归测试记录完整。

---

## 15. 附：建议目录结构

```text
group5/
  coordinator/
    system_coordinator.py
  security/
    security_sandbox.py
    policies.py
  knowledge/
    rag_kb.py
    trace_store.py
  audit/
    audit_logger.py
  contracts/
    schemas.py
    error_codes.py
  tests/
    test_coordinator.py
    test_security.py
    test_rag.py
    test_integration.py
```

该结构用于统一模块边界与测试布局，便于第 3-4 周快速收敛与验收。