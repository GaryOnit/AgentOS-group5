# 第五组联调测试报告

- 日期：2026-09-08
- 当前环境：Windows，Python 3.14.3
- 当前结论：第五组Mock与契约联调通过；真实第1–4组和Linux桌面联调未完成

## 1. 已验证范围

第五组完整测试命令：

```powershell
python -m unittest discover -s tests -p "test_*.py"
```

结果：

```text
Ran 138 tests in 14.337s
OK (skipped=2)
```

已验证：

- v1任务、结果、错误与运行时校验。
- 第一组接口适配的成功和 `1001–1004` 错误映射。
- 多轮补参、确认、过期和取消。
- 第二组组合/分离规划检测接口。
- 第三组检查点、重新检测和工具暂停恢复。
- 第四组工具失败、缺失和重复调用ID。
- SQLite事务、并发、环境隔离和重启恢复。
- 审计递归脱敏、哈希链、导出和整链轮换。
- RAG文档筛选、TF-IDF、语义后端接口和故障降级。
- Linux路径词法规范化和文件能力边界。
- 工具授权签名、篡改、过期和重放。
- Bubblewrap参数构造、失败关闭和Mock模式。
- 文件事务预检查、逆序回滚和部分失败。
- GUI/变更串行、只读并发和协作式取消。

## 2. 跳过项

1. 第一组父目录源码当前不在工作区，真实 `HostAgent` 门面测试自动跳过。
2. 当前Windows权限不允许创建目录符号链接，真实链接逃逸测试自动跳过。

以上跳过项不得写成“已通过”，需要在依赖和Linux环境到位后补测。

## 3. 当前模块模式

| 模块 | 当前可用模式 | 真实联调状态 |
|---|---|---|
| Group1 | 第五组离线契约Mock | 外部源码当前缺失 |
| Group2 | Mock + v1适配器伪实现 | 真实代码未提供 |
| Group3 | Mock + v1检查点伪实现 | 真实代码未提供 |
| Group4 | Mock + 工具协议伪实现 | 真实代码未提供 |
| Group5 | 真实本地实现 | Windows单元/集成测试通过 |

## 4. 尚未完成的验收

- Ubuntu 22.04+/GNOME 42+运行。
- 真实AT-SPI控件检测。
- 真实PyAutoGUI/AppAgent执行。
- 真实第四组文件工具和Bubblewrap命名空间隔离。
- 第一至第四组真实适配器契约测试。
- 六个任务书GUI场景的可见最终状态验证。

## 5. 真实联调准入条件

每个外组代码到位后必须先完成：

1. 阅读README、入口、公共接口和测试。
2. 运行该组自身测试，建立基线。
3. 仅在第五组适配器中转换字段。
4. 对不支持的能力明确标记降级，不伪装成功。
5. 通过对应契约测试后才进入端到端演示。
